<!DOCTYPE ...>   -  this tag defines the document type and HTML Version.
<html>  -this tag encloses the complete HTML document and  mainly comprises of document header which is represented by <head>
....</head> and document body which is represented by <body>....</body> tags
<head>  - this tag represents the documents header which can keep other HTML tags like <title>,<link> etc
<title> - The <title> tag is used inside the <head> tag to mention the document title.
<body> - the tag represents the documents body which keeps other HTML tags like <h1>,<div>,<p> etc.
<h1> -  this tag represents the heading.
<p> - this tag represents paragraph.

<b> - makes text bold
<i>  makes text italic
<strong>  Displays bold text
<em>  Displays italic text
<mark> Highlights text
<q> adds quotation marks
<br> line break
<hr> Horizontal line
<blockquote> Block- level quotation
<cite>  Cites a ssource
<sub> subscript text
<sup> Superscript text
<code> Displays cpde text
<address> contact information
<pre> Performatted text
<u>  Underlines text
<kbd> Keyboard input
<dfn> defination term

vimp for me
text as a link
image as a link
gmail as a link

attributes to search on
autoplay
controls
loop
preload :it has three types
1 none
2 metadata
3 auto

semantic and non-semantic tags
most commonluy nav chai header vitra huncha
article tag chai moe about blog
aside tag is used for extra information
mostly used on add run

block and inline elements
display:none;
positon:static, absolute, relative, fixed, sticky
z-index:
relative:original position lai reference lincha
absolute : ma order dissolve huncha 


1inch=96 px
1inch= 2..5 cm
1 inch=0.25
1 inch=6 fc


units in CSS
1. absolute
a. px
b. in
c. mm
d.cm
e. pt
f. pc

2. relative
em
rem
vw
%
bira ko parent ko font size lai reference lincha
body ko default font-size:16px;
rem le chai body ko default px lincha reference even if specify gare pani
1 rem=16px

flex-direction deaals with main axis
and justify-content controls the main axis
iframes.

useful link for icons https://fontawesome.com/ for this we need to import the link from font awesome from https://cdnjs.com/libraries/font-awesome